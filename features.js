// const gName = "MrsRandom"
// export const bName = "MrRandom"
// export default gName

export const percentLove = () => {
      return `${Math.floor(Math.random()*100)}%`;

      // //Another way to write
      // return `${~~(Math.random()*100)}%`;
}